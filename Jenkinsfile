pipeline {
  agent any 
    stages {
      stage('code Validate'){
	steps {
        sh 'ls -ltr'
        }
      }
    
    stage ('compile') {
       steps {
       sh 'echo "compile is running"'
       }
    }
	
 
  }
}

